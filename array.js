var bucketList = ['learn to juggle', 'take a falconry class', 'climb Mt Everest'];

bucketList.push('score the winning touchdown at the super bowl', 'start an alpaca farm');

console.log(bucketList);
